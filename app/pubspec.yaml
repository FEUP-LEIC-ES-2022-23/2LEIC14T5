name: filter_it
description: App design to help current and future IT workers find their ideal job.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.3.0

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.0
  readmore: ^2.2.0
  go_router: ^6.5.6
  cloud_firestore: ^4.5.1
  firebase_core: ^2.9.0
  firebase_auth: ^4.4.1
  firebase_ui_auth: ^1.2.1
  font_awesome_flutter: 10.4.0
  url_launcher: ^6.1.7
  google_fonts: ^4.0.3
  image_picker: ^0.8.7+3
  firebase_storage: ^11.1.1
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: 5.4.0
  build_runner: 2.3.3
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
